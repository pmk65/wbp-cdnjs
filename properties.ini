[Properties]
Return immediately on exact match=check
Remove URI Scheme=check
Show CDNJS library description=check

[Values]
Return immediately on exact match=1
Remove URI Scheme=1
Show CDNJS library description=1

[Hints]
Return immediately on exact match=If checked and selection is an exact match on the CDNJS library keyword, plugin will return with match without waiting for user to choose from list.
Remove URI Scheme=If checked, "http:" or "https:" will be removed from link.
Show CDNJS library description=If checked, description of CDNJS library will be displayed in autocompleter.
